/**
 * Compatibility adapter: Guard owns its implementation in the standalone package.
 * Codapult keeps the historical `codapult guard ...` command surface.
 */
export * from '@codapult/guard/commands/guard';
